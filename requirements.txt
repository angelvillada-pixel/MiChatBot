# ══════════════════════════════════════════════════════════════════════
# DeepNova v3 PREMIUM — dependencias (optimizado para Railway <4GB)
# ══════════════════════════════════════════════════════════════════════

# Originales (DeepNova v1)
flask==3.0.0
flask-cors==4.0.0
groq>=0.9.0
gunicorn==21.2.0
httpx>=0.27.0
requests>=2.31.0
pypdf>=4.0.0
python-docx>=1.1.0

# ── v2 · M2 (persistencia dual SQLite/Postgres) ────────────────────
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9; python_version < "3.13"

# ── v2 · M2 (embeddings — SOLO numpy, fallback hashing) ────────────
# ❌ sentence-transformers removido: arrastra PyTorch (~2.5GB) y excede el límite de 4GB en Railway
# ✅ embeddings.py cae automáticamente al fallback hashing cuando no está sentence-transformers
numpy>=1.24.0

# ── v2 · M7 (favicon: Pillow directo, sin cairosvg) ────────────────
# ❌ cairosvg removido: requiere libcairo2 a nivel de sistema (complica el build)
# ✅ favicon.py cae automáticamente a Pillow
pillow>=10.0.0
